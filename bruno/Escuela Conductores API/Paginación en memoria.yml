info:
  name: Paginación en memoria
  type: http
  seq: 8

http:
  method: GET
  url: "{{url}}/api/departamentos/memoria?page=0&size=2"
  params:
    - name: page
      value: "0"
      type: query
    - name: size
      value: "2"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
