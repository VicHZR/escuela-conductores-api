info:
  name: Listar HATEOAS
  type: http
  seq: 9

http:
  method: GET
  url: "{{url}}/api/departamentos/hateoas"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
