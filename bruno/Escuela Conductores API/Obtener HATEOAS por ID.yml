info:
  name: Obtener HATEOAS por ID
  type: http
  seq: 10

http:
  method: GET
  url: "{{url}}/api/departamentos/hateoas/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
