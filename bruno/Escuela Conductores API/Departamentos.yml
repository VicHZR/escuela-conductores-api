info:
  name: Departamentos
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/departamentos"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
